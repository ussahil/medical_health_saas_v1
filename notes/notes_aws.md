#### Services 
1) AppRunner : Takes in a docker container  and deploys it to the cloud (Common Starting point)
2) ECR (Elastic Container Registry) : It is like a registry for containers on AWS
3) CloudWatch : (Collect Logs from all your Services)
4) Just added in a new Branch
5) side note clerk does not work with app router (react) hence we had to build/ use page router